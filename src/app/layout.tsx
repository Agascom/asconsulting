import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "A&S Consulting — Cabinet comptable à Libreville",
    template: "%s | A&S Consulting",
  },
  description:
    "Cabinet de gestion comptable, fiscale, sociale et administrative à Libreville. Tenue de comptabilité, fiscalité, paie et création d'entreprise.",
  metadataBase: new URL("https://as-consulting.ga"),
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="fr"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-white text-slate-900">
        {children}
      </body>
    </html>
  );
}
